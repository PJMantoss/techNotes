import React from 'react';
import { Link } from 'react-router-dom';

const DashHeader = () => {

    const content = (
        <header>
            <div>
                <Link></Link>
                <nav></nav>
            </div>
        </header>
    );

  return content;
}

export default DashHeader;